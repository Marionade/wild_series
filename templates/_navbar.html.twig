<nav class="navbar navbar-expand-md navbar-dark bg-dark flex-column">

  <div class="container-fluid">
    <a class="navbar-brand" href="/">Accueil</a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav">
        <li class="nav-item">
        <a class="nav-link" href="/category">Catégories</a>
        </li>      
        
       <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        {{ component('Navbar') }}

        <li class="nav-item">
        <a class="nav-link" href="/program">Séries</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="/actor">Acteurs</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_favoris') }}">Favoris</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/register">S'inscrire</a>
        </li>
          {% if app.user %}
              {{ app.user.email }}
              <a href="{{ path('app_logout') }}"> Se déconnecter</a>
          {% else %}
              <a href="{{ path('app_login') }}"> Se connecter</a>
          {% endif %}
        </li>
        <button class="nav-link" {{ stimulus_action('theme', 'toggleDarkMode') }}>Toggle dark mode</button>
        </div>
      </ul>
  </div>
